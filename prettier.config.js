// ?NOTE: needs to have Prettier config for Zed,
// even tho it doesn't act on source files (ts/js/tsx/jsx) are ignored in prettierignore
// because we use ESLint Stylistic entirely for many lanugages.
export { default } from 'eslint-config-xaxa/prettier';
